source 'https://rubygems.org'
ruby '2.2.2'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.1'

# Use mysql as the database for Active Record
gem 'mysql2'

# Use SLIM as a templating engine
gem 'slim-rails'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'

# Use Bootstrap
gem 'bootstrap-sass'

# Show AJAX loading bar
gem 'nprogress-rails'

# Use JQuery UI
gem 'jquery-ui-rails'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Use Curb to post requests to the groupme API
gem 'curb'

# Use thin as the app server
gem 'thin'

# Use money gem to deal with formatting currency
gem 'money'